```script.js
git clone: creates local copy of a remote
git remote -v: lists a git project's remote
git fetch: fetches work from the remote into the local copy
git merge origin/master: merges origin/master into local branch
git push origin <branch_name>: pushes local branch to the origin remote
```